# Core
fastapi>=0.141.0
uvicorn[standard]>=0.34.0
pydantic>=2.13.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0

# AI / LangChain (pin floor theo phiên bản đã verify 2026-09 — xem cost-management.md về chọn model)
langchain>=1.4.0
langgraph>=1.2.0
# Chọn provider theo model đội dùng (đủ cả 3 để học viên đổi model không đổi code flow):
langchain-openai>=1.6.0        # GPT series
langchain-anthropic>=1.7.0     # Claude series
# langchain-google-genai>=4.4.0 # Gemini series (uncomment nếu dùng)

# Database (uncomment as needed)
# sqlalchemy>=2.0.0
# alembic>=1.14.0
# psycopg2-binary>=2.9.0

# Vector Store (uncomment as needed)
# chromadb>=0.5.0

# Dev tools
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0

# Eval
pyyaml>=6.0
